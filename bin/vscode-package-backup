#!/bin/sh
#
# Usage: vscode-package-backup
#
# Saves a list of your currently installed Visual Studio Code
# packages to ~/.dotfiles/vscode.symlink/packages.txt suitable
# for install via vscode-package-install

set -e

code --list-extensions > ~/.dotfiles/vscode.symlink/packages.txt
